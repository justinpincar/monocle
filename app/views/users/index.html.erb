This is the users index.<br />
<br />
Total users: <%= @users.count %><br />
<br />
<% @users.each do |user| %>
 Id: <%= link_to user._id, user_path(user._id) %> (last seen <%= user.u_at %>)<br />
<% end %>
